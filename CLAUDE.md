# CLAUDE.md

This repo's instructions live in [`AGENTS.md`](AGENTS.md) — the cross-tool
convention (also read by Codex, Cursor, and 20+ other coding agents), so
there's one file to keep current instead of two drifting apart. This file
exists only so Claude Code auto-loads it every session: Claude Code reads
`CLAUDE.md` at session start, not `AGENTS.md`, by default.

@AGENTS.md
